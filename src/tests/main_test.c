// #include "test.h"

// static int run_test(){
//     SRunner *sr = srunner_create(test_suite);

// }

// int main(void) {
//   int failed = 0;
//   printf(GREEN);
//   Suite *suite_array[] = {mul_suite()};

//   for (int i = 0; i < (int)(sizeof(suite_array) / sizeof(suite_array[0])); i++){
//     failed += run_test_suite(suite_array[i]);
//   }

//   printf(RESET);
//   return (failed == 0) ? SUCCESS : FAILURE;
// }